<div class="medium-insert-buttons" contenteditable="false" style="display: none">
  {{#each addons}}
      <span class="medium-insert-buttons-file">
        <a data-addon="{{@key}}" data-action="add" class="medium-insert-action">{{{label}}}</a>
      </span>
  {{/each}}
</div>
